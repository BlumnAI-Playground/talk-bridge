# v1.1.3 — 02 실측 완료 (2026-09-09)

## 요약

02-api-webhook 이 호스티드 게이트웨이 실이벤트·REST 발신·echo 까지 엔드투엔드로 검증되었다.
그 과정에서 드러난 **호스티드 게이트웨이의 이중 `agent` echo** 를 계약 지식에 넣고, REST 스펙을 별도 지식으로 분리했다.
Patch 버전 — 구조 변경 없음.

## 추가

### Layer 1 — knowledge/

| 파일 | 내용 |
|---|---|
| `talkbridge-rest-api-spec.md` | 인증·환경, 샘플이 쓰는 엔드포인트 표, **실측 봉투**(me 봉투 없음 / rooms·messages 최상위 키·최신순), 에러 코드별 재시도 원칙, CLI 에 없는 것·REST 에 없는 것, 검수 포인트 |

## 변경

- `talkbridge-webhook-contract.md` — kind 표와 §3.1 에 **이중 echo** 추가: ① seq 없는 즉시 신호 ② seq 있는 저널 항목. seq 없는 agent 는 저장 금지
- `agents/manual-digest-verifier.md` — B' 항목 신설 (REST 대조: `api.js` ↔ 요약 §5·§7 ↔ 02 README §4, `cli.js`/`api.js` 반환 모델 동일성)
- `docs/README.md` 트리·히스토리, `harness.config.json` 1.1.2 → 1.1.3

## 프로젝트 코드 변경 (하네스 밖 — 공유 파일 규칙에 따라 01·02 동일 적용)

| 파일 | 변경 | 근거 |
|---|---|---|
| `server/webhook.js` (01·02) | `case 'agent'` 에서 `seq == null` 이면 저장하지 않고 SSE `ack` 만 방송 | 이중 echo 로 말풍선 3중 표시 재현 |
| `server/store.js` (01·02) | seq 확정된 agent echo 가 오면 같은 본문의 pending 말풍선 제거 | 낙관적 말풍선이 영구 잔류하던 01 의 잠재 결함 |
| `02/server/index.js` | `TB_DEBUG_WEBHOOK=1` 이면 헤더·원본 본문 로깅 (서명 12자 마스킹) | 페이로드 실측 수단 |
| README 01 §3.3 / 02 §6.5·§6.6, 02 `.env.example` | 위 동작 설명 | |

## 실측 타임라인

1. 문의 2건 수신 (#13, #14) — 서명·멱등·보강 정상
2. 사용자 화면 답장 → 로그에 `agent #undefined` 와 `agent #15` → 말풍선 3중 확인
3. 디버그 로깅 후 `/api/send` 재현 → 페이로드 ① `{userKey,kind,brand}` ② `{…,seq:16}` 확정, `send` 응답에 `serial`
4. 핸들러·저장소 수정 후 재발신 → 검증 결과는 로그 참조

## 다음 버전 후보

- CLI 게이트웨이(01)도 이중 echo 를 보내는지 실측 — 다르면 `talkbridge-webhook-contract.md` 에 게이트웨이별 차이 기록
- `전체 점검해` 로 02 포함 첫 full-review
