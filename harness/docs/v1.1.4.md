# v1.1.4 — Windows 셸 한글 인코딩 (2026-09-09)

## 요약

02 검증 중 `curl -d '…한글…'` 로 보낸 답장이 깨진 채 발송된 사고를 실측으로 분해해 지식화했다.
사용자 질문("인코딩 안 깨지고 보내는 방법 지식 추가되었나?")으로 누락이 드러남 — 로그에 부작용으로만 적고 지식은 안 만들었었다.
Patch 버전.

## 추가

### Layer 1 — knowledge/

| 파일 | 내용 |
|---|---|
| `windows-shell-encoding.md` | 6가지 발신 경로의 도착 바이트 비교표(로컬 에코 서버 실측), 원인(mingw curl 의 ANSI argv, `Invoke-RestMethod` 의 ISO-8859-1 기본), 규칙 6조, 검수 포인트 |

## 변경

- `sample-portability-constraints.md` §2 — 한글 페이로드 항목 추가
- 01·02 README §8 문제 해결 — "curl 한글 깨짐" 행 추가 (보내는 쪽 문제임을 명시)
- `docs/README.md` 트리·히스토리, `harness.config.json` 1.1.3 → 1.1.4

## 교훈 (정원지기)

"부작용" 으로 로그에 남긴 것은 지식이 아니다. 같은 실수를 다음 사람이 반복하지 않게 하려면 **원인·재현·규칙**이 knowledge/ 에 있어야 한다.
사고 → 로그만 남김 → 지식 누락 패턴을 `하네스를 개선해` 시 점검 항목으로 삼는다.
