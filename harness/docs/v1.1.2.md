# v1.1.2 — 02 샘플 착수 (2026-09-09)

## 요약

`sample-project/02-api-webhook` (REST API × 호스티드 웹훅) 을 만들면서 드러난 사실 두 가지를 지식에 반영했다.
Patch 버전 — 구조 변경 없음. 02 자체는 하네스 밖(프로젝트 코드)이며, 기존 에이전트·엔진의 대상 경로(`sample-project/*`)에 자동 포함된다.

## 변경

### Layer 1 — knowledge/

| 파일 | 변경 |
|---|---|
| `talkbridge-cli-spec.md` §1 | **환경 매핑** 추가 — REST 베이스 dev `api.talkbridge-dev.com` / prod `api.talkbridge.io`(파트너 기준 실환경), CLI 코어·센터 주소, "키는 발급 환경에 묶임", 개발환경은 엔터프라이즈 구간에 사전 제공될 수 있음 |
| `sample-portability-constraints.md` §6 | "webhook.js·signature.js 무수정 재사용" 약속을 **실측 표**로 교체 — signature/store/sse 는 바이트 동일, webhook.js 는 코드 2줄(import·호출), cli.js ↔ api.js 는 반환 모델 동일 규칙, 포트 8787/8788 |

### 프로젝트 문서 정정 (하네스 밖, 문서 대조관 관점)

- `sample-project/README.md`, `01-cli-gateway/README.md` §0 — "한 줄도 안 고치고" → 실제(서명·저장소·SSE 무수정, webhook.js 두 줄)로 정정
- 루트 `README.md`, `sample-project/README.md` 표 — 02 상태 🚧 예정 → 🧪 검증 중

## 02 착수 시점의 검증 상태

| 항목 | 상태 |
|---|---|
| 로컬 서명 수신 (401/200/멱등) | ✅ |
| 인터넷 경로 (Funnel 인그레스 → 8788) 401 | ✅ |
| `doctor` 6종 (실패 케이스 4종 포함) | ✅ |
| REST `me`/`rooms`/`messages` 실제 응답 봉투 | ✅ 최상위 키 (`talkbridge-webhook-contract.md` §3.1) — `pick()` 1순위에서 적중 |
| 호스티드 게이트웨이 실이벤트 수신 | ✅ 문의 2건(#13, #14) 서명 통과·멱등·본문 보강 정상 |
| 발신(REST `send`) + `agent` echo | ⏳ 과금 동작이라 사용자 결정 대기 |

## 다음 버전 후보

- 실측 완료 후 `talkbridge-webhook-contract.md` 에 호스티드 게이트웨이 실이벤트(헤더·재시도 관측) 추가, `api.js` 정규화 고정
- `full-review` 를 02 포함으로 한 번 실행 — 특히 문서 대조관이 01↔02 README 교차 검증
