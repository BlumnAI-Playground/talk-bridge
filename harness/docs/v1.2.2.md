# v1.2.2 — CLI v1.1.0 재검수: cli-feat 상태 갱신 + 03 샘플 재개 (2026-09-09)

## 요약

dev 채널 CLI 가 v1.1.0 으로 올라 cli-feat 개선 예고 10건이 반영되었다. 개선 수집관이 각 카드의 **수용 기준을 실제로 실행**해
상태를 갱신하고, 03 샘플을 v1.1.0 기준으로 다시 썼다(조회 창 제거, `--json`, `query --write`). 재검수 중 처음 관찰된 현상 1건(CF-011)을
`cli-feat/v1.1.0/` 에 신설했다. Patch 버전 — 지식 갱신·문서 보강.

## 재검수 결과 (dev v1.1.0)

| # | 결과 | 핵심 근거 |
|---|---|---|
| CF-001 잠금 | 구현됨 | 게이트웨이 조회 API 8790, CLI 자동 경유, 03 창 제거 후 프리셋 10종 통과 |
| CF-002 query 안전 | 구현됨 | `--write` 없으면 거부, `#n FAILED` + exit 1, `--continue-on-error` |
| CF-003 build ts | 구현됨 | `ts` = `timestampUnixMs`(1ms 반올림), `loadedAt` 추가 |
| CF-004 AI 선택 | 구현됨 | AI 없이 install, status 표기 변경, 자연어 search 안내 (AI 없는 build 실측은 생략) |
| CF-005 setup --help | 구현됨 | config 무변경, `--dry-run`. 잔여 → CF-011 |
| CF-006 view | 구현됨 | 127.0.0.1 OK, graph.json 실노드, /search JSON, /cypher, cytoscape 로컬 |
| CF-007 --json | 구현됨 ⚠ | 전역 `--json`, 연도 표기. **연도 추가로 01·03 텍스트 파서 0건** → 01 호환 수정, 03 `--json` 전환 |
| CF-008 이중 echo | 미반영 | 원시 로거 실측: ① seq 없음 ② 285ms 뒤 seq 18, `phase`/`serial` 없음 |
| CF-009 문서·REST | 미반영 | REST `history`/`delete` 404, `/api/reference` 404 |
| CF-010 shim | 부분 | `env --json` launcherPath/binaryPath ✓, 자식 잔존 없음 ✓, 문서 ✗ |
| CF-011 (신규) | 예고 | `knowledge view/build/search --help` 가 명령을 실행 |

## 변경

### Layer 1 — knowledge/

- `cli-feat/README.md` — 기준 v1.1.0, 인덱스 상태 11행, 파트너 안내 한 줄, 디렉터리 규칙 보완("처음 관찰한 버전")
- `cli-feat/v1.0.0/CF-001~010` — 상태 갱신 + "재검수" 절(명령·출력 인용)
- `cli-feat/v1.1.0/CF-011-knowledge-subcommand-help.md` — 신설
- `talkbridge-cli-spec.md` §3 (`--json` 형태 표, 연도 호환 검수 포인트), §4 (`env --json`), §5 (knowledge v1.1.0 표, `--help` 주의)
- `talkbridge-knowledge-graph.md` §2~§5 — 조회 API·쓰기 창·`--json`·`query --write`·AI 선택으로 재서술, 스키마에 `loadedAt`

### 샘플

- **03** — `server/cli.js` (`--json` + 버전 검사), `server/graph.js` (조회 창 제거, 다중 컬럼), `scripts/write-window.mjs` (신설, 쓰기 때만 게이트웨이 정지),
  `seed.mjs` (`--write`), `knowledge.mjs`, `doctor.mjs` ([2] v1.1.0+ 검사, [8] AI 선택), `index.js`/`app.js`/`index.html` 문구, `.env.example`,
  README §0·§2·§4·§5(제약→해소 표)·§6·§7·§8 + "TalkBridge CLI — 그래프 확장편" 절과 스크린샷 `docs/screenshot-graph.png`
- **01** — `server/cli.js` 텍스트 파서가 v1.0.0/v1.1.0 타임스탬프 둘 다 수용, README §6.2·표 갱신 (v1.0.0 운영 채널 호환 유지)
- 루트·샘플 인덱스 README — 03 소개(그래프 확장편, 스크린샷), CLI 버전 문구

## 유지한 것

- 01 은 텍스트 파서 유지 — 운영 채널 `talkbridge` 가 아직 v1.0.0 이라 `--json` 전환은 03 에서만
- 02 `webhook.js` 의 `seq == null` 분기 — CF-008 미반영이라 그대로
- 이 PC 의 CLI config 에 남은 AI 자리표시자 — 04 샘플 전에 실제 프로바이더로 교체 (`knowledge-graph.md` §5)

## 다음 버전 후보

- CF-008·009·011 이 반영된 CLI/문서가 나오면 `개선사항 수집해` 로 재검수
- 01 을 `--json` 으로 전환할지는 운영 채널 v1.1.0 출시 후 결정
- `전체 점검해` 로 03 을 이식 감독관·문서 대조관에 다시 태워 v1.1.0 서술 드리프트 확인
