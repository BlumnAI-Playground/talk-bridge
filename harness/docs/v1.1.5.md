# v1.1.5 — 03 샘플 착수: CLI `knowledge` 지식 (2026-09-09)

## 요약

`sample-project/03-cli-knowledge-graph` (01 복제 + 상담지식 그래프 탐색) 착수. 01 은 무수정 —
그래프가 필요 없는 파트너를 위해 분리 복제. CLI `knowledge` 명령군과 `setup --help` 함정을 지식에 반영. Patch 버전.

## 변경

### Layer 1 — knowledge/

| 파일 | 변경 |
|---|---|
| `talkbridge-cli-spec.md` | §5 `knowledge` 하위 명령 표, AI 프로바이더 전제, 미설치 오류 문구, 읽기전용 검수 포인트, **`setup --help` 는 setup 을 실행한다** 함정 (기존 §5 인증·과금 → §6) |

### 프로젝트 (하네스 밖)

- `03-cli-knowledge-graph/` — 01 복제 후: `cli.js` knowledge 래퍼 4종 + status 파서, `/api/knowledge/*` 3 라우트,
  오른쪽 열 분할 UI(그래프 패널 + 고객 그래프 버튼), `doctor [8]` + `[4]` sink 포트 불일치 경고, `scripts/knowledge.mjs`, 기본 포트 8789
- 루트·sample-project README 표에 03 🧪 (AI 프로바이더 대기), sample-project README 에 03 절

## 03 착수 시점의 검증 상태

| 항목 | 상태 |
|---|---|
| 8789 부팅, 01 기능(백필·SSE·서명) | ✅ (01 과 동일 코드) |
| `/api/knowledge/status` 파싱, search 가드(미설치 502 / 비-MATCH Cypher 400) | ✅ |
| 게이트웨이 sink → 8789 재구성, 데몬 기동 | ✅ |
| `knowledge install` / `build` / `search` 실출력 | ⏳ **AI 프로바이더 설정 대기** — 키는 사용자가 `setup --ai-*` 로 직접 |
| 검색 결과 형식 → 카드 렌더링 | ⏳ 실출력 확인 후 |

## 다음 버전 후보

- 실측 후 `talkbridge-cli-spec.md` §5 에 search 출력 형식·그래프 스키마(노드/관계 라벨) 기록, 03 README §5 채우기
- 03 의 `고객 그래프` 버튼을 스키마 확정 후 Cypher 템플릿으로 교체 검토
